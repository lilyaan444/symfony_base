{% extends 'base.html.twig' %}

{% block title %}Liste des burgers{% endblock %}
{% block body %}

    <h1>Liste des burgers</h1>
     <ul>
        {% for burger in burgers %}
            <li>
                <strong>{{ burger.name }}</strong> - Prix: {{ burger.price }}€
                <ul>


                </ul>
            </li>
        {% else %}
            <li>Aucun burger trouvé.</li>
        {% endfor %}
    </ul>

   <a href="{{ path('burger_new') }}" class="btn btn-success">Créer un nouveau burger</a>

{% endblock %}
